# .zprofile is similar to .zlogin, except that it is sourced before .zshrc.
# .zprofile is meant as an alternative to .zlogin for ksh fans;
# the two are not meant to beintended to be used together, although this could
# certainly be done if desired.

# Add pyenv executable to PATH and
# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$HOME/.pyenv/shims/python:$PATH"
# export PATH="$PYENV_ROOT/bin:$PATH"
# eval "$(pyenv init --path)"
